# todoops_app_vm_ip from vars.yml (terraform output todoops_app_vm_public_ip)

all:
  children:
    todoops:
      hosts:
        todoops_app_vm:
          ansible_host: "{{ todoops_app_vm_ip | default('REPLACE_WITH_TODOOPS_APP_VM_PUBLIC_IP') }}"
